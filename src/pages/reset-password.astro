---
import Footer from "../components/Footer.astro";
import Header from "../components/Header.astro";
import Layout from "../layouts/Layout.astro";
---

<Layout>
	<Header />

	<main class="page">
		<section class="reset-password">
			<div class="login-container rounded-4">
				<div class="top-container text-center">
					<h1 class="title mb-1 text-primary-grad-1">Reset Password</h1>
				</div>

				<form class="reset-form" novalidate>
					<div class="form-div w-full mb-3">
						<label for="email" class="form-label">Email Address</label>
						<p class="feedback__patient">John_07@gmail.com</p>
						<div class="invalid-feedback">Please enter a valid email.</div>
					</div>

					<!-- New Password Field -->
					<div class="form-div w-full mb-3 position-relative">
						<label for="new_password" class="form-label">New Password</label>
						<input
							type="password"
							class="basic-input form-control form-control-lg pe-5"
							id="new_password"
							required
						/>
						<i
							class="icon-StateOn icon-pos"
							id="toggleNewPassword"
							style="z-index: 10; font-size: 1.25rem; color: #6b46c1;"></i>
						<div class="invalid-feedback">Password must not be empty.</div>
					</div>

					<!-- Confirm Password Field -->
					<div class="form-div w-full mb-3 position-relative">
						<label for="confirm_password" class="form-label"
							>Confirm Password</label
						>
						<input
							type="password"
							class="basic-input form-control form-control-lg pe-5"
							id="confirm_password"
							required
						/>
						<i
							class="icon-StateOn icon-pos"
							id="toggleConfirmPassword"
							style="z-index: 10; font-size: 1.25rem; color: #6b46c1;"></i>
						<div class="invalid-feedback">Passwords must match.</div>
					</div>

					<div class="btn-div">
						<button
							type="submit"
							class="theme-btn theme-btn__primary w-100 btn-lg mb-4"
						>
							Change Password
						</button>

						<div class="text-center mt-3">
							<span>Already have an account?</span>
							<a
								href="#"
								class="text-link text-decoration-underline forgot-text"
								>Log In</a
							>
							<p class="first-time-text mt-3">
								First-time Visitor?
								<a
									href="#"
									class="text-link text-decoration-underline fw-medium"
									>Create an account</a
								>
							</p>
						</div>
					</div>
				</form>
			</div>
		</section>
	</main>

	<script>
		const togglePassword = (inputId: string, iconId: string) => {
			const input = document.getElementById(inputId) as HTMLInputElement;
			const icon = document.getElementById(iconId);

			if (!input || !icon) return;

			const isPassword = input.type === "password";
			input.type = isPassword ? "text" : "password";

			// Toggle icon
			icon.classList.toggle("icon-StateOff", !isPassword);
			icon.classList.toggle("icon-StateOn", isPassword);
		};

		// CORRECT: Listen on the ICON, not the input!
		document
			.getElementById("toggleNewPassword")
			?.addEventListener("click", () => {
				togglePassword("new_password", "toggleNewPassword");
			});

		document
			.getElementById("toggleConfirmPassword")
			?.addEventListener("click", () => {
				togglePassword("confirm_password", "toggleConfirmPassword");
			});
	</script>
	<Footer />
</Layout>
